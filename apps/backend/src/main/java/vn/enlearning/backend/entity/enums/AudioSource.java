package vn.enlearning.backend.entity.enums;

/** Nguồn file audio của bài nghe: sinh bằng TTS từ transcript, do Admin tải lên, hoặc audio người đọc thật (Tatoeba, seed 13f). */
public enum AudioSource {
	TTS, UPLOAD, REAL
}
